<h1><%= @category.name %></h1>

<div class="smallLink">
	<a href="/welcome">...Back</a>
</div>

<div class="articles">
	
	 <% @category.articles.each do |article|%>
	 	<h2> <a href="/edit/<%= article.id %>"><%= article.subject %></a>  Click to Edit</h2>
	 	<p> <%= article.post %> </p>
	 	<div>
	 		<img src="<%= article.img_url %>">
	 	</div>

	 	<h4>by <a href="/profile/<%= article.user.id %>"><%= article.user.user_name %></a> on <%= article.post_date %> </h4>
	 <% end %>
</div>

<div class="smallLink">
	<a href="/welcome">...Back</a>
</div>